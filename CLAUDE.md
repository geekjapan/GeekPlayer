# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

**AGENTS.md is the single source of truth** for project structure, the OpenSpec workflow, build/test commands, multi-chat governance, coding style, commit/PR conventions, and graphify usage. Edit AGENTS.md, not this file, when those change.

@AGENTS.md

## Claude Code specific

- The `/opsx:*` slash commands are defined under `.claude/commands/opsx/` (mirrored for other harnesses under `.agents/`, `.codex/`, `.pi/`).
