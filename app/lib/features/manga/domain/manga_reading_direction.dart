/// Reading direction for manga.
enum MangaReadingDirection {
  /// Pages advance from right to left (traditional Japanese manga).
  rightToLeft,

  /// Pages advance from left to right (Western comics).
  leftToRight,
}
